# buddy-bridge Python dependencies.
#
# The hub (buddyhub.py), the hook agent (buddy-hook.py), and install_hooks.py are
# stdlib-only — nothing to install for those.

# Relay host (the machine with the Bluetooth radio): BLE transport for relay.py
bleak

# Optional — only needed to (re)generate the tty character pack via build_tty.py
Pillow
